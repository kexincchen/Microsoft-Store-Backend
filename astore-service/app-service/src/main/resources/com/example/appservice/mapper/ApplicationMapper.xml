<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.appservice.mapper.ApplicationMapper">

    <select id="selectApplicationById" resultType="com.example.appservice.entity.Application">
        SELECT * FROM applications WHERE id = #{id}
    </select>

    <!-- Define more SQL statements as needed -->

</mapper>

